schema_version = "1.0.0"

id = "dotcraft_bridge"
version = "0.1.1"
name = "DotCraft Bridge"
tagline = "Let DotCraft inspect and drive this Blender session"
maintainer = "DotHarness <https://github.com/DotHarness>"
type = "add-on"
website = "https://github.com/DotHarness/dotcraft-blender"
license = ["SPDX:GPL-2.0-or-later"]
blender_version_min = "4.2.0"
tags = ["Development", "Pipeline"]

[permissions]
network = "Accepts token-authenticated connections from DotCraft on the loopback interface."
files = "Writes viewport and render images to the temporary directory."
